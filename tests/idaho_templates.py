IdahoRead = {
    "edges": [],
    "nodes": [
        {
            "id": "IdahoRead",
            "operator": "IdahoRead",
            "parameters": {
                "bucketName": "${bucketName}",
                "imageId": "${imageId}",
                "objectStore": "${objectStore}",
                "targetGSD": "${targetGSD}"
            }
        }
    ]
}
